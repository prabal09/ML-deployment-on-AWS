# -------------------------------------------------------------
# Core Dependencies (Standard Versioning)
# -------------------------------------------------------------
requests==2.31.0          # Exact match: strict pinning for production stability
numpy>=1.24.0             # Minimum version: allows updates but requires at least 1.24.0
pandas<=2.2.0             # Maximum version: prevents breaking changes from newer updates
scikit-learn~=1.3.0       # Compatible release: equivalent to >=1.3.0 and <1.4.0